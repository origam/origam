INSERT INTO "OrigamFeatureList" ("Id", "ReferenceCode", "DisplayName", "Description", "IsFeatureOn", "RecordCreated") 
select gen_random_uuid(), 'NET_MEMBERSHIP_EXCLUSIVE_USE', 'User Management Extensions',
'When turned on new buttons will be available in the user management for adding/removing/unlocking users and changing password. Use this feature only when the configured membership provider supports these functions.',false, now()
where not exists (select 1 from "OrigamFeatureList" where "ReferenceCode"='NET_MEMBERSHIP_EXCLUSIVE_USE')
