@inject IHtmlLocalizer<SharedResources> SharedLocalizer
@using Microsoft.AspNetCore.Mvc.Localization
@using Origam.Server
@model Origam.Server.IdentityServerGui.Account.LogoutViewModel

<div class="detailview-container">
    <div class="detailview-module with-title">
        <form asp-action="Logout">
            <input type="hidden" name="logoutId" value="@Model.LogoutId"  />
            <h2>@SharedLocalizer["ConfirmLogout"]</h2>
            <div class="input-form">
                <div class="input-row">
                    <a id="yesButton" class="btn normal-size primary submit-link" value="" href=# >@SharedLocalizer["Yes"]</a>
                </div>
            </div>
        </form>
    </div>
</div>