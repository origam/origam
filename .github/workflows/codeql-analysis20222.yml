name: "Backend and Architect 2022.2"

on:
  push:
    branches: [ '2022.2' ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ '2022.2' ]

jobs:
  analyze:
    name: Analyze
    runs-on: windows-2022
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'csharp']

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      with:
          ref: 2022.2

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v2
      with:
        languages: ${{ matrix.language }}

    - name: setup-msbuild
      uses: microsoft/setup-msbuild@v1
    
    - name: Build Backend
      run: |
       msbuild backend/Origam.sln -t:restore /p:Configuration="Release Server"
       msbuild backend/Origam.sln /p:UseSharedCompilation=false /p:Configuration="Release Server" /t:Server\Origam_ServerCore:Publish -v:m

    - name: Build Architect
      run: |
       msbuild backend/Origam.sln -t:restore /p:Configuration="Release Architect"
       msbuild backend/Origam.sln /p:Configuration="Release Architect"
       
    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v2
