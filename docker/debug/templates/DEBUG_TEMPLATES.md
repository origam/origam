Files in this directory are useful for developing and testing the docker images. To use them, copy them to `origam\docker\debug` (one level up) and replace the following placeholders:
- `<path-to-origam-repo>`
- `<db-user>`
- `<db-password>`
- `<db-name>`
